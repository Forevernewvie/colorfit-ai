name: personal_color_app
description: "ColorFit AI - AI Personal Color Analysis & Pastone Camera"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.6.1
  google_mobile_ads: ^5.1.0
  image_picker: ^1.1.2
  camera: ^0.11.0+1
  share_plus: ^9.0.0
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
